pluginManagement {
    repositories {
        maven(uri("https://jitpack.io"))
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "News"
include (":app")


